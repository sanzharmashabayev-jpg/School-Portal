# School Portal - School Portal System

Веб-приложение портала школы с аутентификацией через Supabase.

## 🚀 Быстрый старт

1. Установите зависимости:
```bash
npm install
```

2. Настройте Supabase:
   - Создайте проект на [Supabase](https://app.supabase.com)
   - Скопируйте `.env.example` в `.env`
   - Заполните значения `VITE_SUPABASE_URL` и `VITE_SUPABASE_ANON_KEY` из настроек проекта Supabase

3. Запустите dev сервер:
```bash
npm run dev
```

## 🔐 Настройка аутентификации Supabase

### 1. Создание проекта Supabase

1. Перейдите на [https://app.supabase.com](https://app.supabase.com)
2. Создайте новый проект
3. В настройках проекта (Settings > API) найдите:
   - Project URL → `VITE_SUPABASE_URL`
   - anon/public key → `VITE_SUPABASE_ANON_KEY`

### 2. Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```env
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
```

### 3. Настройка пользователей в Supabase

1. Перейдите в Authentication > Users в панели Supabase
2. Создайте пользователей через "Add user" или "Sign up"
3. Для администраторов:
   - Либо используйте email, заканчивающийся на `@admin.school.ru`
   - Либо добавьте в метаданные пользователя поле `role: 'admin'`

### 4. Создание администратора через метаданные

В Supabase Dashboard:
1. Authentication > Users > выберите пользователя
2. User Metadata:
   ```json
   {
     "role": "admin"
   }
   ```

### 5. Тестовые учетные данные

Для быстрого тестирования используйте готовые тестовые аккаунты.  
📋 См. подробности: [TEST_ACCOUNTS.md](./TEST_ACCOUNTS.md)

**Быстрый доступ:**
- **Обычный пользователь:** `test@school.ru` / `test123456`
- **Администратор:** `admin@admin.school.ru` / `admin123456`

⚠️ Не забудьте создать этих пользователей в Supabase Dashboard перед использованием!

## 📁 Структура проекта

```
src/
├── contexts/
│   └── AuthContext.tsx      # Контекст аутентификации
├── lib/
│   └── supabase.ts          # Конфигурация Supabase клиента
├── pages/
│   ├── Login.tsx            # Страница входа
│   ├── AdminLogin.tsx       # Страница входа администратора
│   └── ...
└── components/
    └── ...
```

## 🔑 Особенности аутентификации

- **Обычные пользователи**: Могут войти через `/login`
- **Администраторы**: Могут войти через `/admin/login`
- **Защита маршрутов**: Автоматическая проверка сессий Supabase
- **Роли**: Определяются через метаданные пользователя или email

## 📝 Команды

- `npm run dev` - Запустить dev сервер
- `npm run build` - Собрать для продакшена
- `npm run preview` - Предпросмотр продакшен сборки
- `npm run lint` - Проверка кода

## 🌐 Подключение собственного домена и деплой

См. подробную инструкцию: [DEPLOYMENT.md](./DEPLOYMENT.md)

### Быстрые варианты деплоя:
- **Vercel** (рекомендуется) - бесплатный хостинг с автоматическим деплоем
- **Netlify** - простой деплой через drag & drop
- **VPS/VDS** - полный контроль над сервером
- **GitHub Pages** - для статических сайтов

## 🔒 Безопасность

- Никогда не коммитьте файл `.env` в Git
- Используйте environment variables для всех секретных ключей
- Для продакшена используйте Row Level Security (RLS) в Supabase
- Используйте HTTPS (SSL) для всех продакшен доменов
